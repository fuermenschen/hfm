---
paths:
    - database/seeders/DatabaseSeeder.php
---

# Seeders

## DatabaseSeeder is development-only

DatabaseSeeder intentionally overwrites invoice settings with local fixture values. Production deployment never runs it; do not change this to preserve configured production values.
