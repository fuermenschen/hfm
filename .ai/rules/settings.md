---
paths:
    - "app/Settings/**"
---

# Settings

## Settings supply admin metadata

Settings extend Spatie Settings, define static group(), and provide settingsDetails(), rules(), titles(), and descriptions() when exposed through custom admin settings UI. Add encrypted() for secrets and options() for dynamic selectors.
